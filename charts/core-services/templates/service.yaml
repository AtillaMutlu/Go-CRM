apiVersion: v1
kind: Service
metadata:
  name: {{ include "core-services.fullname" . }}
  labels:
    app: {{ include "core-services.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ include "core-services.name" . }} 